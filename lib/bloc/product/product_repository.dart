import 'package:pluis/models/api_models/dio_helper.dart';
import 'package:pluis/models/api_models/product.dart';

class ProductRepository {
  /* Future<List<Product>> findAll() async {
    var response =
        await DioHelper.get(cachedPetition: true, url:"https://calzadopluis.com/admin/v1/product-settings/");
    if (response.statusCode == 200) {
      var answer = response.data["items"];
      List<Product> products = List();

      for (var i = 0; i < answer.length; i++) {
        products.add(Product.fromJson(answer[i]));
      }
    }
  }*/
}
